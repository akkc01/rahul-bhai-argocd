apiVersion: v1
kind: Service
metadata:
  name: rahul-service
spec:
  selector:
    app: rahul-deployment
  ports:
  - port: 80    # service port
    targetPort: 80 # target port on the pod
  type: LoadBalancer